<%= form_for(@stop_type) do |f| %>
  <% if @stop_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stop_type.errors.count, "error") %> prohibited this stop_type from being saved:</h2>

      <ul>
      <% @stop_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
